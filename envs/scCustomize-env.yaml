name: scCustomize-env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base
  - r-seurat
  - bioconductor-complexheatmap
  - bioconductor-dittoseq
  - bioconductor-dropletutils
  - bioconductor-nebulosa
  - cairo
  - r-ggrastr
  - r-viridis
  - r-dplyr
  - r-svglite

# DoubletFinder is not available using conda so install using the below command
# remotes::install_github('chris-mcginnis-ucsf/DoubletFinder')
